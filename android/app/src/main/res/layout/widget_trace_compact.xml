<?xml version="1.0" encoding="utf-8"?>
<!-- Compact variant for small widgets (2 cells or fewer on either axis):
     the photo fills the whole widget, the caption stays hidden, and the
     camera button shrinks so it doesn't cover the picture. Same view ids
     as widget_trace.xml so RemoteViews calls never miss a target. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg">

    <ImageView
        android:id="@+id/photo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone"
        android:contentDescription="@string/app_name" />

    <TextView
        android:id="@+id/caption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:gravity="center"
        android:padding="6dp"
        android:background="#33FFFFFF"
        android:textColor="#3B352E"
        android:textSize="11sp"
        android:maxLines="1"
        android:visibility="gone" />

    <TextView
        android:id="@+id/status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="10dp"
        android:textColor="#3B352E"
        android:textSize="12sp"
        android:lineSpacingMultiplier="1.3"
        android:visibility="gone" />

    <ImageButton
        android:id="@+id/cameraBtn"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="bottom|right"
        android:layout_margin="8dp"
        android:background="@drawable/circle_camera"
        android:src="@drawable/ic_camera"
        android:padding="9dp"
        android:scaleType="fitCenter"
        android:contentDescription="@string/app_name"
        android:visibility="gone" />
</FrameLayout>
